# Backend
backend = "glx";
vsync = true;
glx-use-copysubbuffer-mesa = true;

glx-copy-from-front = true;
glx-swap-method = 2;
xrender-sync = true;
xrender-sync-fence = true;

# Fading
# fading = true;
# fade-in-step = 0.075;
# fade-out-step = 0.10;
# fade-delta = 7;
